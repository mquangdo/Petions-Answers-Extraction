services:
  # rabbitmq:
  #   image: rabbitmq:3-management
  #   container_name: rabbitmq
  #   restart: always
  #   ports:
  #     - "5672:5672"     # AMQP - API/worker nối vào đây
  #     - "15672:15672"   # Management UI (trình duyệt)
  #   environment:
  #     RABBITMQ_DEFAULT_USER: "admin"
  #     RABBITMQ_DEFAULT_PASS: "admin123"

  # minio:
  #   image: minio/minio:latest
  #   container_name: minio_petitions
  #   restart: unless-stopped
  #   command: server /data --console-address ":9001"

  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin123
  #     # MINIO_SERVER_URL: "https://9002--main--dev--sinhnq3.coder.vts-ai.space"

  #   ports:
  #     - "9002:9000"
  #     - "9003:9001"

  #   volumes:
  #     - minio_petitions_data:/data

  #   healthcheck:
  #     test: ["CMD", "mc", "ready", "local"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 10s

  db:
    image: postgres:13
    container_name: postgres-dev
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_jobs_data:/var/lib/postgresql
      
volumes:
  minio_petitions_data:
  postgres_jobs_data:
  file_store: